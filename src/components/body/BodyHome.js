const BodyHome = (props) => {
  //   console.log(props);
  return (
    <p>
      This card will contain a default home component until a dog is clicked, in
      which case the dog component will be displayed instead with the relevant
      dog data passed in
    </p>
  );
};

export default BodyHome;
